#include "main.h"
/**
 * main - function that print its name followed by new line
 * @argc: number of commands arguments
 * @argv: array that holds the arguments prompted by user when run the program
 * Return: returns 0 success
 */
int main(__attribute__((unused))int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
