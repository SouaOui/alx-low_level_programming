#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;

	/*
	 * this is a random comment
	 */
	for (c = 'a'; c <= 'z' ; c++)
	{
	putchar(c);
	}
	putchar('\n');
	return (0);
}
